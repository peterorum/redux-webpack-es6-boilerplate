import React, { Component } from 'react';

import './Word.scss';

export default class WordLayout extends Component {
  static propTypes = {
  };

  render() {

    return (
      <div className='word'>
        fishier
      </div>
    );
  }
}
